export const metadata = {
  title: "Login — North Avenue Backstage",
  description: "Log ind for at se jobs, løn og bookinger.",
};

export default function LoginLayout({ children }) {
  return children;
}
